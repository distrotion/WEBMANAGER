# Windows scripts must use CRLF so Windows PowerShell / cmd parse them correctly.
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

# Keep shell scripts LF for macOS/Linux.
*.sh text eol=lf

# Binaries — never touch line endings.
*.exe binary
