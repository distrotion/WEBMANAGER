# PROMPT: ออกแบบระบบดึงรูปจากกล้อง 172.26.20.72 ผ่าน FTP

> คัดลอกทั้งบล็อกไปวางเป็น context/system prompt แล้วเติมโจทย์ท้ายสุด
> ข้อมูลด้านล่างแยกชัดว่าอันไหน **ยืนยันแล้วด้วยการทดสอบจริง** อันไหน **ยังไม่รู้ ต้องไปเช็คก่อน**

---

คุณกำลังออกแบบระบบดึงรูปจากอุปกรณ์ปลายทางผ่าน FTP เข้ามาเก็บ/ใช้งานต่อในระบบ QC ของโรงงาน

## 1. ข้อเท็จจริงที่ยืนยันแล้ว (ทดสอบจริง 2026-09-04)

**อุปกรณ์ต้นทาง**
| รายการ | ค่า |
|---|---|
| IP:port | `172.26.20.72:10021` (FTP) |
| Banner | `220 Service ready` (ไม่บอกยี่ห้อ/รุ่น) |
| ผู้ใช้ | `Admin` · **รหัสผ่านว่าง** → ตอบ `230 User logged in` |
| โปรโตคอล | **plain FTP เท่านั้น** — `AUTH TLS`/`AUTH SSL` → ปฏิเสธแล้วตัดสาย |
| โฟลเดอร์เริ่มต้นหลัง login | `/EM/` |
| เนื้อในนั้น | โฟลเดอร์เดียวชื่อ **`VS`** (mtime 29 Jan 15:23) |
| โหมดข้อมูล | passive · ประกาศ **IP ของตัวเอง + พอร์ตสุ่ม** (เห็นจริง: 60052, 60329) |

**เครือข่าย — ข้อจำกัดสำคัญที่สุดของงานนี้**
| จาก | ถึงกล้อง | ผลจริง |
|---|---|---|
| **Windows Server `172.23.10.34`** | ตรง | ✅ ใช้ได้ครบ — login + list + โอนไฟล์ (passive ทำงานปกติ) |
| เครื่องผู้ใช้ (subnet `172.18.x`) ผ่าน gateway ของ .34 พอร์ต 10021 | ผ่านตัวกลาง | ⚠️ login ผ่าน แต่ **list/โอนไฟล์ไม่ได้** — พอร์ต passive ที่กล้องประกาศไม่มีใคร forward → timeout |
| เครื่องผู้ใช้ ตรงไปกล้อง | ตรง | ❌ ไม่มี route ไปถึง subnet `172.26.20.x` |

→ **ตัวดึงรูปต้องรันบน .34 เท่านั้น** (หรือเครื่องอื่นที่อยู่ในเส้นทางเดียวกับกล้อง) จะออกแบบให้ดึงจากเครื่องผู้ใช้โดยตรงไม่ได้

**พฤติกรรม path ของ curl ที่ต้องระวัง** — path ใน URL เป็น relative กับโฟลเดอร์ที่ login เข้ามา:
```
curl.exe ftp://172.26.20.72:10021/EM/  --user "Admin:"   →  ❌ curl: (9) denied  (กลายเป็น /EM/EM)
curl.exe ftp://172.26.20.72:10021/     --user "Admin:"   →  ✅ list /EM/
curl.exe ftp://172.26.20.72:10021/VS/  --user "Admin:"   →  ✅ list /EM/VS/
curl.exe ftp://172.26.20.72:10021//EM/ --user "Admin:"   →  absolute path (ต้องมี // ถ้าจะระบุจาก root)
curl.exe -l <url>                                        →  เอาเฉพาะชื่อไฟล์ ไม่เอา metadata
```
ลงท้ายด้วย `/` เสมอเมื่อจะ list โฟลเดอร์ ไม่งั้น curl ตีความว่าเป็นไฟล์

## 2. ยังไม่รู้ — ต้องไปเช็คก่อนออกแบบ

1. **ในโฟลเดอร์ `VS` มีอะไร** — มีไฟล์รูปตรงๆ หรือซ้อนอีกหลายชั้น (เช่น แยกตามวัน/กะ)
2. **รูปแบบชื่อไฟล์** — มี timestamp ในชื่อมั้ย เรียงลำดับได้มั้ย (ตัวชี้ขาดว่าจะ "ดึงเฉพาะของใหม่" ยังไง)
3. **จำนวน/ขนาดไฟล์ และอัตราการเกิดใหม่** — กี่รูป/วัน รูปละกี่ MB (กำหนดพื้นที่ดิสก์ + ความถี่การดึง)
4. **กล้องลบไฟล์เก่าเองมั้ย** หรือกองสะสมจนเต็มแล้วหยุดเขียน
5. **ลบไฟล์บนกล้องได้/ควรมั้ย** หลังดึงมาแล้ว (กล้องยอมให้ DELE รึเปล่า และเจ้าของสายการผลิตอนุญาตมั้ย)
6. **ปลายทางของรูป** — เอาไปทำอะไรต่อ (ให้คนดู / ให้ ML เทรน / แนบใน report QC)

## 3. ของที่มีอยู่แล้วในระบบ ใช้ต่อได้เลย (WEBMANAGER บน .34)

อย่าสร้างใหม่ทับของเดิม — ตรวจก่อนว่าอันไหนใช้แทนได้:

| ของที่มี | ใช้ทำอะไรได้ในงานนี้ |
|---|---|
| **FTP server** (`:21`) + บัญชีแยก root ต่อบัญชี | ให้เครื่องอื่นมาหยิบรูปที่ดึงมาแล้ว · มีบัญชี `autopackliquid` root `C:\Users\Administrator\Desktop\autopackliquid` อยู่แล้ว |
| **หน้า browser ในตัว panel** (FTP server → 📂) | เปิดดู/โหลด/ลบรูปผ่านเว็บ พร้อม preview รูป — ไม่ต้องเขียนหน้าใหม่ |
| **File Share** (HTTP read-only + api-token) | ให้สคริปต์ ML ดึงรูปแบบ list ทั้ง tree แล้ว fetch ทีละไฟล์ |
| **Monitor** | เฝ้าว่ากล้องยังตอบอยู่มั้ย (tcp/http) + แจ้งเตือนตอนล่ม |
| **Message Queue** (HTTP publish/pull/ack) | คิวงาน "มีรูปใหม่" ให้ตัวประมวลผลตามไปทำ โดยไม่ต้องให้ตัวดึงรอ |
| **PM2 site** | ที่รันตัวดึงเองแบบมี auto-restart + log ในตัว |
| **Auto-deploy / logs / audit / webhook notify** | deploy โค้ดตัวดึง + ดู log + แจ้งเตือน |

ทั้งหมดจัดการผ่าน panel `http://172.23.10.34:8088` (Windows Server 2019, ROOT `C:\webmanager`)

## 4. กับดักที่ต้องออกแบบให้รอด (เจอมาแล้วจริงในระบบนี้)

1. **ดึงซ้ำไม่รู้จบ** — ถ้าไม่จำว่าดึงอะไรไปแล้ว ทุกรอบจะโหลดทั้งโฟลเดอร์ใหม่หมด ต้องมีตัวชี้ว่า "ไฟล์นี้ได้แล้ว" (ชื่อ+ขนาด+mtime หรือฐานข้อมูลเล็กๆ)
2. **ดิสก์เต็มเงียบ** — รูปกองสะสมไม่มี retention แล้วเซิร์ฟเวอร์ตายทั้งเครื่อง ไม่ใช่แค่งานนี้ตาย (.34 รันอีก ~9 ระบบอยู่)
3. **ไฟล์ที่กล้องยังเขียนไม่เสร็จ** — ดึงตอนเขียนค้างจะได้รูปเสียครึ่งใบ ควรเช็คขนาดนิ่งก่อน หรือดึงเฉพาะไฟล์ที่เก่ากว่า N วินาที
4. **กล้องล่ม/สายหลุดกลางคัน** — ต้อง fail แล้วลองใหม่รอบหน้า **ห้ามเขียนทับไฟล์ดีด้วยไฟล์ที่โหลดไม่ครบ** (โหลดลงชื่อชั่วคราวแล้วค่อย rename)
5. **log ท่วม** — เขียน log ทุกไฟล์ทุกรอบ = แสนบรรทัด/วัน ให้ log เฉพาะตอนเปลี่ยนสถานะ/มีของใหม่/พลาด (บทเรียนจาก Monitor)
6. **งานค้างทับกัน** — รอบก่อนยังไม่เสร็จ รอบใหม่มาแล้ว ต้องมี lock กันซ้อน
7. **กล้องไม่มีรหัสผ่าน** — ใครถึง `172.26.20.72:10021` ได้ก็อ่าน/เขียน/ลบไฟล์กล้องได้หมด ระบบที่ออกแบบไม่ควรทำให้ช่องนี้กว้างขึ้นโดยไม่ตั้งใจ (เช่น เปิด forward ให้ทั้งวงโดยไม่จำเป็น)
8. **อย่าให้ตัวดึงล้มแล้วลาก panel ล้มตาม** — subsystem ที่พังต้องพังเฉพาะตัวเอง

## 5. คำสั่งที่ทดสอบแล้วว่าใช้ได้จริง (รันบน .34)

```cmd
:: list โฟลเดอร์ราก (= /EM/)
curl.exe ftp://172.26.20.72:10021/ --user "Admin:"

:: list ใน VS
curl.exe ftp://172.26.20.72:10021/VS/ --user "Admin:"

:: เอาเฉพาะชื่อไฟล์
curl.exe -l ftp://172.26.20.72:10021/VS/ --user "Admin:"

:: โหลดไฟล์เดียว
curl.exe ftp://172.26.20.72:10021/VS/<ไฟล์> --user "Admin:" -o "D:\path\<ไฟล์>"
```

```powershell
# โหลดทั้งโฟลเดอร์ (ตัวอย่างดิบ — ยังไม่มีกันซ้ำ/กันไฟล์เขียนค้าง ต้องออกแบบเพิ่ม)
$src = "ftp://172.26.20.72:10021/VS/"
$dst = "C:\Users\Administrator\Desktop\autopackliquid\VS"
mkdir $dst -Force | Out-Null
(curl.exe -s -l $src --user "Admin:") -split "`n" | ? { $_.Trim() } | % {
  $f = $_.Trim(); curl.exe -s "$src$f" --user "Admin:" -o (Join-Path $dst $f)
}
```

## 6. สิ่งที่ต้องส่งมอบตอนออกแบบเสร็จ

- ตัวดึงรันที่ไหน รูปแบบไหน (สคริปต์ตามเวลา / service ค้างไว้ / ฟีเจอร์ใน webmanager)
- ดึงบ่อยแค่ไหน และรู้ได้ยังไงว่าอันไหนใหม่
- รูปไปเก็บที่ path ไหน จัดโฟลเดอร์ยังไง เก็บนานเท่าไหร่ ลบยังไง
- ใครมาหยิบต่อ และหยิบด้วยช่องทางไหน (FTP ของเรา / File Share / คิว)
- ล้มเหลวแล้วรู้ได้ยังไง (log/monitor/แจ้งเตือน) และกู้ยังไง
- ประเมินพื้นที่ดิสก์ที่ใช้ต่อเดือน

---

## โจทย์

<!-- เขียนโจทย์จริงตรงนี้ -->
